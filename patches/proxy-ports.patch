diff --git a/proxy/main.go b/proxy/main.go
index a043a34..3c2fe3f 100644
--- a/proxy/main.go
+++ b/proxy/main.go
@@ -16,6 +16,7 @@ import (
 	"net"
 	"net/http"
 	"os"
+	"strconv"
 	"strings"
 	"sync"
 	"sync/atomic"
@@ -28,7 +29,10 @@ type DomainPolicy struct {
 	Methods  map[string]bool
 }
 
-// Config maps domain names to their policies. The "*" key is the default policy.
+// Config maps allowlist entries to their policies. An entry is a domain
+// ("example.com") or a domain and a port ("example.com:8443"). The "*" key is
+// the default policy. An entry without a port covers the default ports only.
+// Write an IPv6 literal in brackets ("[::1]:8443").
 type Config map[string]DomainPolicy
 
 // Redirects maps a lowercase hostname to a local "host:port" address.
@@ -100,6 +104,11 @@ func loadConfig(path string) (Config, error) {
 	cfg := make(Config)
 	for domain, val := range raw {
 		domain = strings.ToLower(domain)
+		if _, port := splitEntry(domain); port != "" {
+			if n, err := strconv.Atoi(port); err != nil || n < 1 || n > 65535 {
+				fmt.Fprintf(os.Stderr, "WARNING: invalid port in allowlist entry %q\n", domain)
+			}
+		}
 		// Try string first ("*"), then array of methods
 		var star string
 		if err := json.Unmarshal(val, &star); err == nil {
@@ -127,40 +136,71 @@ func loadConfig(path string) (Config, error) {
 	return cfg, nil
 }
 
-// lookupPolicy finds the policy for a host, falling back to the "*" default.
-// When multiple suffix entries match, the longest (most specific) wins.
-func lookupPolicy(host string, cfg Config) (DomainPolicy, bool) {
-	host = strings.ToLower(host)
-	if p, ok := cfg[host]; ok {
-		return p, true
+// defaultPorts are the ports that an allowlist entry covers when the entry
+// names no port: 443 for CONNECT (TLS) and 80 for plaintext HTTP.
+var defaultPorts = map[string]bool{"80": true, "443": true}
+
+// splitEntry splits an allowlist entry into its domain and port parts. An
+// entry without a port gives an empty port.
+func splitEntry(entry string) (string, string) {
+	domain, port, err := net.SplitHostPort(entry)
+	if err != nil {
+		return entry, ""
+	}
+	return domain, port
+}
+
+// entryPortMatches reports whether an allowlist entry covers a request port.
+// An entry without a port covers the default ports only.
+func entryPortMatches(entryPort, port string) bool {
+	if entryPort == "" {
+		return defaultPorts[port]
 	}
-	// Collect all suffix matches and pick the longest (most specific).
-	var bestDomain string
+	return entryPort == port
+}
+
+// lookupPolicy finds the policy for a host and port, falling back to the "*"
+// default. When more than one entry matches, the longest (most specific)
+// domain wins. An exact domain beats a suffix match of the same length, and an
+// entry with an explicit port beats an entry without one.
+func lookupPolicy(host, port string, cfg Config) (DomainPolicy, bool) {
+	host = strings.ToLower(host)
 	var bestPolicy DomainPolicy
-	for d, p := range cfg {
-		if d != "*" && strings.HasSuffix(host, "."+d) {
-			if len(d) > len(bestDomain) {
-				bestDomain = d
-				bestPolicy = p
-			}
+	bestScore := -1
+	for entry, policy := range cfg {
+		domain, entryPort := splitEntry(entry)
+		if !entryPortMatches(entryPort, port) {
+			continue
+		}
+		var score int
+		switch {
+		case domain == "*":
+			score = 0
+		case host == domain:
+			score = 4*len(domain) + 2
+		case strings.HasSuffix(host, "."+domain):
+			score = 4 * len(domain)
+		default:
+			continue
+		}
+		if entryPort != "" {
+			score++
+		}
+		if score > bestScore {
+			bestScore = score
+			bestPolicy = policy
 		}
 	}
-	if bestDomain != "" {
-		return bestPolicy, true
-	}
-	if p, ok := cfg["*"]; ok {
-		return p, true
-	}
-	return DomainPolicy{}, false
+	return bestPolicy, bestScore >= 0
 }
 
-func isDomainAllowed(host string, cfg Config) bool {
-	_, ok := lookupPolicy(host, cfg)
+func isDomainAllowed(host, port string, cfg Config) bool {
+	_, ok := lookupPolicy(host, port, cfg)
 	return ok
 }
 
-func isMethodAllowed(host, method string, cfg Config) bool {
-	policy, ok := lookupPolicy(host, cfg)
+func isMethodAllowed(host, port, method string, cfg Config) bool {
+	policy, ok := lookupPolicy(host, port, cfg)
 	if !ok {
 		return false
 	}
@@ -334,13 +374,13 @@ func hasRequestBody(req *http.Request) bool {
 // restrictions. Returns an HTTP status code and reason if blocked, or 0 if
 // allowed. Callers must check isDomainAllowed first; this only applies
 // per-request filters.
-func applyFilters(req *http.Request, host string, cfg Config) (int, string) {
+func applyFilters(req *http.Request, host, port string, cfg Config) (int, string) {
 	// Normalise once and use the same value for every check below. Comparing
 	// the uppercased method against the policy but the raw one against the
 	// GET/HEAD checks let "-X get" satisfy a GET policy and then skip the
 	// restrictions that policy is supposed to carry.
 	normalizedMethod := strings.ToUpper(req.Method)
-	if !isMethodAllowed(host, normalizedMethod, cfg) {
+	if !isMethodAllowed(host, port, normalizedMethod, cfg) {
 		return http.StatusForbidden, "method not allowed"
 	}
 	if requestURLLength(req) > maxURLBytes {
@@ -471,34 +511,32 @@ func handle(conn net.Conn, cfg Config, ca *certAuthority, redirects Redirects) {
 	}
 
 	host := hostOnly(req.Host)
+	port := portOf(req.Host)
 
 	if req.Method == http.MethodConnect {
-		if portOf(req.Host) != "443" {
-			fmt.Fprintf(os.Stderr, "%s blocked non-443 CONNECT: %s\n", time.Now().Format(time.RFC3339), req.Host)
-			fmt.Fprintf(conn, "HTTP/1.1 403 Forbidden\r\n\r\n")
-			return
+		// A CONNECT target names its port. Assume 443 if the client omits it.
+		if port == "" {
+			port = "443"
 		}
-		if !isDomainAllowed(host, cfg) {
+		if !isDomainAllowed(host, port, cfg) {
 			fmt.Fprintf(os.Stderr, "%s blocked domain: %s\n", time.Now().Format(time.RFC3339), req.Host)
 			fmt.Fprintf(conn, "HTTP/1.1 403 Forbidden\r\n\r\n")
 			return
 		}
 		// MITM: intercept the TLS connection to inspect HTTP requests
 		fmt.Fprintf(conn, "HTTP/1.1 200 Connection Established\r\n\r\n")
-		handleMITM(conn, host, req.Host, cfg, ca, redirects)
+		handleMITM(conn, host, net.JoinHostPort(host, port), port, cfg, ca, redirects)
 	} else {
-		if p := portOf(req.Host); p != "" && p != "80" {
-			fmt.Fprintf(os.Stderr, "%s blocked non-80 plaintext: %s\n", time.Now().Format(time.RFC3339), req.Host)
-			fmt.Fprintf(conn, "HTTP/1.1 403 Forbidden\r\n\r\n")
-			return
+		if port == "" {
+			port = "80"
 		}
 		// Plaintext HTTP — check domain first, then apply full filtering
-		if !isDomainAllowed(host, cfg) {
+		if !isDomainAllowed(host, port, cfg) {
 			fmt.Fprintf(os.Stderr, "%s blocked domain: %s\n", time.Now().Format(time.RFC3339), req.Host)
 			fmt.Fprintf(conn, "HTTP/1.1 403 Forbidden\r\n\r\n")
 			return
 		}
-		if code, reason := applyFilters(req, host, cfg); code != 0 {
+		if code, reason := applyFilters(req, host, port, cfg); code != 0 {
 			fmt.Fprintf(os.Stderr, "%s blocked %s %s (%s, host: %s)\n",
 				time.Now().Format(time.RFC3339), req.Method, req.URL, reason, req.Host)
 			fmt.Fprintf(conn, "HTTP/1.1 %d %s\r\n\r\n", code, http.StatusText(code))
@@ -522,7 +560,7 @@ func handle(conn net.Conn, cfg Config, ca *certAuthority, redirects Redirects) {
 			// Same substitution as the redirect above, but with a vetted
 			// literal, so the transport dials the address that was checked
 			// instead of resolving the name a second time.
-			vetted, err := resolveVetted(host, "80")
+			vetted, err := resolveVetted(host, port)
 			if err != nil {
 				code := dialFailureStatus(err)
 				fmt.Fprintf(os.Stderr, "%s blocked %s %s (%v)\n",
@@ -547,7 +585,7 @@ func handle(conn net.Conn, cfg Config, ca *certAuthority, redirects Redirects) {
 	}
 }
 
-func handleMITM(clientConn net.Conn, host, hostPort string, cfg Config, ca *certAuthority, redirects Redirects) {
+func handleMITM(clientConn net.Conn, host, hostPort, port string, cfg Config, ca *certAuthority, redirects Redirects) {
 	// Mint a certificate for this host
 	leafCert, err := ca.mintCert(host)
 	if err != nil {
@@ -617,7 +655,7 @@ func handleMITM(clientConn net.Conn, host, hostPort string, cfg Config, ca *cert
 		}
 
 		// Apply filters to the decrypted request
-		if code, reason := applyFilters(req, host, cfg); code != 0 {
+		if code, reason := applyFilters(req, host, port, cfg); code != 0 {
 			fmt.Fprintf(os.Stderr, "%s blocked %s https://%s%s (%s)\n",
 				time.Now().Format(time.RFC3339), req.Method, host, req.URL.Path, reason)
 			resp := &http.Response{
diff --git a/proxy/main_test.go b/proxy/main_test.go
index fd71aa4..4f0ae1d 100644
--- a/proxy/main_test.go
+++ b/proxy/main_test.go
@@ -192,7 +192,7 @@ func TestApplyFilters(t *testing.T) {
 	for _, c := range cases {
 		t.Run(c.name, func(t *testing.T) {
 			req := readRequest(t, c.raw)
-			status, reason := applyFilters(req, "example.com", c.cfg)
+			status, reason := applyFilters(req, "example.com", "443", c.cfg)
 			if status != c.status {
 				t.Errorf("applyFilters = (%d, %q), want status %d", status, reason, c.status)
 			}
